# 任务单：只读技术栈总结（Smoke Test）

## 背景

本任务是主控架构验证 DeepSeek worker 调用链的 smoke test。
项目是 FreightTalent / ACE-Talent，一个货代垂直招聘平台。

## 目标

仅读取以下两个文件，总结项目技术栈，**不允许修改任何文件**。

- `/Users/edy/Desktop/货代招聘/AGENTS.md`
- `/Users/edy/Desktop/货代招聘/package.json`

输出一份结构化的技术栈报告，**只打印到你的回复中**，不写入任何文件。

## 允许修改文件

无。**本任务禁止写入或修改任何文件。**

## 禁止修改文件

- 所有文件（无例外）

## 实现步骤

1. 读取 `AGENTS.md`（重点看第 2 节"技术栈"和第 11 节"启动方式"）。
2. 读取 `package.json`（查看 dependencies / devDependencies）。
3. 整合两份信息，输出以下格式的技术栈总结。
4. **停止。不创建文件，不执行任何命令，不修改代码。**

## 验收标准

- [ ] 技术栈报告已在回复中输出
- [ ] `git diff` 与执行前完全一致（无任何文件变动）
- [ ] 报告中包含以下 6 个维度：前端框架、构建工具、样式方案、前端路由、HTTP 客户端、后端

## 必须运行命令

无。本任务**禁止运行任何 shell 命令或写文件命令**。

## 输出要求

在你的回复中打印如下格式（Markdown 表格）：

```
## ACE-Talent 技术栈总结

| 层 | 技术 | 版本 |
|---|---|---|
| 前端框架 | ... | ... |
| 构建工具 | ... | ... |
| 样式方案 | ... | ... |
| 前端路由 | ... | ... |
| HTTP 客户端 | ... | ... |
| 实时通信 | ... | ... |
| 后端框架 | ... | ... |
| 数据库 | ... | ... |
| 认证 | ... | ... |
| 图表 | ... | ... |

## 架构亮点
- （2-3 条关键设计决策）
```

## 停止条件

输出完技术栈报告后**立即停止**，不做其他任何事情。
